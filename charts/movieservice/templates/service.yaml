apiVersion: v1
kind: Service
metadata:
  name: {{ include "movieservice.fullname" . }}
  labels:
    {{- include "movieservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "movieservice.selectorLabels" . | nindent 4 }}
