apiVersion: v1
kind: Service
metadata:
  name: {{ include "castservice.fullname" . }}
  labels:
    {{- include "castservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "castservice.selectorLabels" . | nindent 4 }}
